<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <div>
            <a href="{{ url('home') }}">home</a>
            <a href="{{ url('calculator') }}">calculator</a>
            <a href="{{ url('post_index') }}">post</a>
            <a href="{{ url('comment_index') }}">comment</a>
            <a href="{{ url('user_index') }}">user</a>
        </div>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
